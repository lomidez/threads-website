<!--
    jQuery Validation Library:
    This script loads the jQuery Validation plugin, which provides client-side validation for forms.
    It helps in enforcing validation rules like required fields, proper formats, etc., without needing to reload the page.
-->
<script src="~/lib/jquery-validation/dist/jquery.validate.js"></script>

<!--
    jQuery Validation Unobtrusive Plugin:
    This script enables unobtrusive validation, which integrates jQuery Validation with ASP.NET's data annotations.
    It allows the use of HTML5 attributes (e.g., `data-val="true"`) to define validation rules directly in the markup.
    The script ensures validation is handled client-side without intrusive pop-ups, keeping the user experience smooth.
-->
<script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js"></script>
